name: Lint
on:
    pull_request:
    schedule:
        - cron: '0 0 * * 1'
jobs:
    lint:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - name: Install modules
              run: yarn
            - name: Run ESLint
              run: npm run lint
