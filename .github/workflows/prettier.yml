name: Prettier

on:
  pull_request:

jobs:
    prettier:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - name: Install modules
              run: yarn
            - name: Run Prettier
              run: npm run prettier
